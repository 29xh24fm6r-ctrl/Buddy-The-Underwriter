#!/usr/bin/env tsx
import { runComplianceCheck } from "../src/lib/brokerage/compliancePackage";
console.log("COMPLIANCE CHECK"); console.log("Module present. No DB — structural only."); process.exit(0);
