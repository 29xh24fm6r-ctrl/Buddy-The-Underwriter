# SPEC-M1.1 migration worklist — entries may only be REMOVED, never added.
# Each path is a pre-existing (pre-gateway) call site that talks to an LLM
# provider endpoint/SDK directly, found by scripts/guards/guard-ai-gateway-
# only.mjs at SPEC-M1 AI-GATEWAY-1 implementation time (2026-07-29). One
# call site (src/app/api/deals/[dealId]/recovery/naics-suggest/route.ts)
# was migrated onto the gateway as SPEC-M1's proof-of-concept and is
# deliberately NOT on this list — the guard now protects it. SPEC-M1.1
# burns the rest of this list down, mechanically, one call site at a time.
# A stale entry (no longer matching the guard's check) FAILS the guard, so
# this list can only shrink.
#
# The 4 voice/realtime files that were originally on this list have moved
# to the PERMANENT EXEMPTION section below (SPEC-GATEWAY-CAPABILITY-
# EXPANSION-1 §5) — they were never real migration debt, see that
# section's header for why.
#
# models.ts / vertexLocation.ts / sdkResponseGuard.ts were removed from
# this list entirely (SPEC-M1.1) — they never had a real call site; they
# were only here because guard-ai-gateway-only.mjs is a literal text scan
# (its own doc comment says so) and each file's doc comments happened to
# quote a banned pattern string verbatim (a URL, an SDK package name) while
# describing real incident history. Reworded those specific phrases
# (meaning preserved, e.g. "the public Gemini REST endpoint" instead of the
# literal domain) rather than either silently keeping them allowlisted
# forever or deleting real incident-history documentation to dodge a
# regex.
#
# openaiClient.ts / gcpAdcBootstrap.ts moved to the PERMANENT EXEMPTION
# section below for the opposite reason — they DO contain real code that
# trips the guard, but that code has nothing to do with the 30 files this
# worklist is migrating; see that section for why.
#
# SPEC-M1.1 complete (2026-07-30): all 25 real migration targets are now
# routed through the gateway — this worklist section is intentionally
# empty. Only the PERMANENT EXEMPTION entries below remain.

# PERMANENT EXEMPTION (SPEC-GATEWAY-CAPABILITY-EXPANSION-1 §5, extended by
# SPEC-M1.1) — NOT migration debt, do not attempt to "finish" migrating
# these.
#
# Voice/realtime (SPEC-GATEWAY-CAPABILITY-EXPANSION-1 §5): these 4 files
# mint/hold a live OpenAI Realtime WebRTC voice session
# (src/app/api/deals/[dealId]/voice/token/route.ts mints the client_secret;
# VoiceInterviewButton.tsx and useBuddyVoice.ts each hold the client-side
# WebRTC session directly against api.openai.com/v1/realtime/calls;
# mintRealtimeClientSecret.ts is the shared token-minting helper both voice
# routes call). A live bidirectional realtime session is architecturally
# not a single request/response completion — there is nothing for
# runRole()/runRoleStream() to wrap.
src/app/api/deals/[dealId]/voice/token/route.ts
src/components/deals/VoiceInterviewButton.tsx
src/lib/voice/mintRealtimeClientSecret.ts
src/lib/voice/useBuddyVoice.ts
#
# Gateway's own auth/SDK plumbing (SPEC-M1.1): unlike every other entry in
# this file, these 2 are used BY the gateway itself, not by a pre-gateway
# call site awaiting migration.
#   - src/lib/ai/openaiClient.ts: `import OpenAI from "openai"` serves ~37
#     pre-gateway callers that never directly matched the guard's banned
#     patterns themselves (they only import getOpenAI() from here) — most
#     of those callers are outside SPEC-M1.1's 30-file worklist entirely.
#     This file can only leave the allowlist once every one of those ~37
#     callers stops using it and it's deleted — a separate, much larger
#     project than migrating this worklist's 30 files.
#   - src/lib/gcpAdcBootstrap.ts: its runVertexAdcSmokeTest() ops function
#     (gated non-production, or an explicit GCP_ADC_SMOKE_TEST=true flag)
#     does a real `await import("@google/genai")` for a manual auth smoke
#     test — unrelated to any of the 30 files this worklist migrates.
#     src/lib/ai/providers/google.ts's Vertex auth path (SPEC-GATEWAY-
#     CAPABILITY-EXPANSION-1 §1) already reuses this file's WIF plumbing
#     via getVertexAccessToken(), not the SDK smoke test.
src/lib/ai/openaiClient.ts
src/lib/gcpAdcBootstrap.ts
