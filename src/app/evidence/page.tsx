import ShellPage from "@/components/stitch/ShellPage";

export const dynamic = "force-dynamic";

export default function EvidencePage() {
  return (
    <ShellPage
      title="Evidence"
      subtitle="Inbox, OCR, excerpts, and structured findings."
      active="evidence"
    />
  );
}
