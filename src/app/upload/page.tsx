import ShellPage from "@/components/stitch/ShellPage";

export const dynamic = "force-dynamic";

export default function UploadPage() {
  return (
    <ShellPage
      title="Upload"
      subtitle="Drop documents here to start evidence extraction."
    />
  );
}
