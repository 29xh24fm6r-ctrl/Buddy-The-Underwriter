import DemoAccessClient from "@/components/admin/DemoAccessClient";

export const metadata = {
  title: "Demo Access | Buddy Admin",
  description: "Invite-only demo access and usage telemetry",
};

export default function DemoAccessPage() {
  return <DemoAccessClient />;
}
